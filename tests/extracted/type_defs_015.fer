// SYNTAX TEST "source.ferlium" "Extracted from type_defs.rs:338"

enum Action {
    Quit,
    Jump(float),
    Move { x: float, y: float },
}

let f = |a| match a {
    Quit => 0.0,
    Jump(h) => h,
    Move { y, x } => x - y,
};

f(Action::Move { x: 30.0, y: 40.0 }) + f(Action::Jump(5.0)) + f(Action::Quit)

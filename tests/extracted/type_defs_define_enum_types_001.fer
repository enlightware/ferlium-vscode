// SYNTAX TEST "source.ferlium" "Extracted from type_defs.rs:define_enum_types:29"

// Basic unit variants
enum SimpleColor {
    Red,
    Green,
    Blue
}

// Single-type variants
enum Option {
    Some(int),
    None
}
enum Option2 {
    Some(int),
    None,
}

// Multi-type tuple variants
enum Player {
    Basic(string),
    Positioned(string, int, int),
    FullData(string, int, int, float)
}

// Record-style variants
enum Event {
    Click { x: int, y: int },
    KeyPress { key: string, shift: bool },
    Resize { width: int, height: int }
}

// Mixed variants in same enum
enum Message {
    Quit,
    Move { x: int, y: int },
    Write(string),
    ChangeColor(int, int, int),
    Callback((int) -> int)
}

// Empty enum
enum Empty {}

// SYNTAX TEST "source.ferlium" "Extracted from type_defs.rs:enum_projections:356"

enum Action {
    Move1 { x: float, y: float },
    Move2 { x: float, y: float },
}

let f = |a| match a {
    Move1 { x, .. } => x,
    Move2 { y, .. } => -y,
};

f(Action::Move1 { x: 30.0, y: 40.0 }) + f(Action::Move2 { x: 30.0, y: 40.0 })

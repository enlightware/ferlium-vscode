// SYNTAX TEST "source.ferlium" "Extracted from complex.rs:record_wildcards_in_match:106"

enum Action {
    Move { x: float, y: float }
}
match (Action::Move { x: 5, y: 6 }) {
    Move { x, .. } => x
}
